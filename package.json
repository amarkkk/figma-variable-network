{
  "name": "color-variable-network-explorer",
  "version": "1.0.0",
  "description": "Figma plugin to visualize color variable relationships",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "typescript": "^5.3.3"
  }
}